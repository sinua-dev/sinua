// Generated by scripts/codegen/generate.mjs from spec/parameters.json -- do not edit.
// Regenerate: node scripts/codegen/generate.mjs (CI fails when this file is stale).

import * as React from "react";
import { View } from "react-native";
import { SinuaView, type SinuaViewProps } from "../SinuaView";
import { sinuaSignalOverrides, sinuaSpecError, type SinuaSize, type SinuaSignalParams, type SinuaSignalPattern } from "./SinuaParams";

type Options = Omit<SinuaViewProps, "spec" | "pattern" | "state" | "specState" | "size" | "overrides" | "inputs" | "voiceLevelInput" | "crossFade">;

/** Signal, typed: `<SinuaSignal pattern="signaling" />` plus any of its parameters. */
export type SinuaSignalProps = Options &
  (
    | (SinuaSignalParams & {
        pattern: SinuaSignalPattern;
        size?: SinuaSize;
        /** The agent's lifecycle state: the built-in voice-state behaviour moves this pattern. */
        state?: string;
        inputs?: Record<string, number>;
        spec?: undefined;
      })
    | {
        /** An FX Spec (JSON text or object) describing a signal; anything else draws nothing and calls `onError`. */
        spec: string | object;
        /** The spec's lifecycle state. */
        state?: string;
        inputs?: Record<string, number>;
        voiceLevelInput?: string;
        crossFade?: number;
        pattern?: undefined;
      }
  ) & { onError?: (message: string) => void };

/** A thin wrapper over SinuaView (no painting of its own). */
export function SinuaSignal(props: SinuaSignalProps) {
  const specError = props.spec != null ? sinuaSpecError(props.spec, "signal") : null;
  const { onError } = props;
  React.useEffect(() => {
    if (specError) (onError ?? console.error)(`SinuaSignal: ${specError}`);
  }, [specError, onError]);
  if (props.spec != null) {
    const { spec, state, onError: _e, pattern: _p, ...rest } = props;
    if (specError) return <View style={rest.style} />;
    return <SinuaView {...rest} spec={spec} state={state} />;
  }
  const { pattern, size, state, inputs, onError: _e, ...rest } = props;
  const overrides = sinuaSignalOverrides(pattern, rest as SinuaSignalParams);
  const options: Record<string, unknown> = { ...rest };
  for (const k of PARAM_KEYS) delete options[k];
  return <SinuaView {...(options as Options)} pattern={pattern} size={size} state={state} inputs={inputs} overrides={overrides} />;
}

const PARAM_KEYS = ["barCount","barWidth","hue","ink","minHeight","saturation","layerCount","lineWidth","pointCount","amplitude","fadeWidth","columnCount","ledCount","ledSize","minLevel","mirror","glow","noise","pulse","gradient","color","liquid","particles","holographic"];
