// Generated by scripts/codegen/generate.mjs from spec/parameters.json -- do not edit.
// Regenerate: node scripts/codegen/generate.mjs (CI fails when this file is stale).

package dev.sinua.view.generated

import android.util.Log
import androidx.compose.foundation.layout.Spacer
import androidx.compose.runtime.Composable
import androidx.compose.runtime.LaunchedEffect
import androidx.compose.ui.Modifier
import dev.sinua.view.FxFrameStats
import dev.sinua.view.FxLowPower
import dev.sinua.view.FxReducedMotion
import dev.sinua.view.FxTheme
import dev.sinua.view.SinuaView
import dev.sinua.voice.VoiceOverrides
import dev.sinua.voice.VoiceSource

/** Core patterns. */
enum class SinuaCorePattern(val id: String) {
    GENERATING("generating"),
    TYPING("typing"),
}

/** Core parameters; null keeps the pattern's value. [toOverrides] is what [SinuaCore] hands to SinuaView. */
data class SinuaCoreProps(
    val pattern: SinuaCorePattern,
    val size: SinuaSize = SinuaSize.S64,
    /** Draw the highlight as one smooth gradient band instead of stepped layers. Patterns: generating. */
    val highlightFill: Boolean? = null,
    /** Highlight length as a fraction of the track. Range 0.05...1 (fraction). Patterns: generating. */
    val highlightLength: Double? = null,
    /** Ink hue in degrees; applies once saturation is above 0. Range 0...360 (deg). */
    val hue: Double? = null,
    /** How present the whole visual is: 1 draws it as the pattern defines it, lower fades everything including its halos. A voice assistant rests below 1 when idle and comes to full ink when it listens. Range 0...1 (fraction). */
    val ink: Double? = null,
    /** How far the ink moves from grey toward the hue (0 = the grey ink). Range 0...1 (fraction). */
    val saturation: Double? = null,
    /** Track thickness as a fraction of the frame. Range 0.01...0.5 (fraction). Patterns: generating. */
    val thickness: Double? = null,
    /** Opacity of the track behind the highlight (0 = highlight only). Range 0...1 (fraction). Patterns: generating. */
    val trackOpacity: Double? = null,
    /** Seconds per highlight sweep. Range 0.05...30 (s). */
    val period: Double? = null,
    /** Track length as a fraction of the frame. Range 0.1...1 (fraction). Patterns: generating. */
    val length: Double? = null,
    /** Bounce height as a fraction of the frame. Range 0...0.5 (fraction). Patterns: typing. */
    val bounceAmplitude: Double? = null,
    /** Seconds between one dot's bounce and the next. Range 0...5 (s). Patterns: typing. */
    val delay: Double? = null,
    /** Number of bouncing dots. Range 1...6. Patterns: typing. */
    val dotCount: Int? = null,
    /** Dot radius as a fraction of the frame. Range 0.01...0.3 (fraction). Patterns: typing. */
    val dotSize: Double? = null,
    /** Gap between dots as a fraction of the frame. Range 0.02...0.5 (fraction). Patterns: typing. */
    val spacing: Double? = null,
    val glow: SinuaGlow? = null,
    val noise: SinuaNoise? = null,
    val pulse: SinuaPulse? = null,
    val gradient: SinuaGradient? = null,
    val color: SinuaColor? = null,
    val liquid: SinuaLiquid? = null,
    val particles: SinuaParticles? = null,
    val holographic: SinuaHolographic? = null,
) {
    fun toOverrides(): Map<String, Double> {
        val o = LinkedHashMap<String, Double>()
        highlightFill?.let { o["highlightFill"] = if (it) 1.0 else 0.0 }
        highlightLength?.let { o["highlightLength"] = it }
        hue?.let { o["hue"] = it }
        ink?.let { o["ink"] = it }
        saturation?.let { o["saturation"] = it }
        thickness?.let { o["thickness"] = it }
        trackOpacity?.let { o["trackOpacity"] = it }
        period?.let { o["period"] = it }
        length?.let { o["length"] = it }
        bounceAmplitude?.let { o["bounceAmplitude"] = it }
        delay?.let { o["delay"] = it }
        dotCount?.let { o["dotCount"] = it.toDouble() }
        dotSize?.let { o["dotSize"] = it }
        spacing?.let { o["spacing"] = it }
        glow?.writeTo(o)
        noise?.writeTo(o)
        pulse?.writeTo(o)
        gradient?.writeTo(o)
        color?.writeTo(o)
        liquid?.writeTo(o)
        particles?.writeTo(o)
        holographic?.writeTo(o)
        return o
    }
}

/**
 * Core, typed: `SinuaCore(pattern = SinuaCorePattern.GENERATING)` plus any of its
 * parameters. A thin wrapper over SinuaView (no painting of its own).
 */
@Composable
fun SinuaCore(
    pattern: SinuaCorePattern,
    modifier: Modifier = Modifier,
    size: SinuaSize = SinuaSize.S64,
    highlightFill: Boolean? = null,
    highlightLength: Double? = null,
    hue: Double? = null,
    ink: Double? = null,
    saturation: Double? = null,
    thickness: Double? = null,
    trackOpacity: Double? = null,
    period: Double? = null,
    length: Double? = null,
    bounceAmplitude: Double? = null,
    delay: Double? = null,
    dotCount: Int? = null,
    dotSize: Double? = null,
    spacing: Double? = null,
    glow: SinuaGlow? = null,
    noise: SinuaNoise? = null,
    pulse: SinuaPulse? = null,
    gradient: SinuaGradient? = null,
    color: SinuaColor? = null,
    liquid: SinuaLiquid? = null,
    particles: SinuaParticles? = null,
    holographic: SinuaHolographic? = null,
    speed: Double = 1.0,
    /**
     * The agent's lifecycle state ("listening", "speaking", ...): the built-in voice-state
     * behaviour then moves this pattern, under the parameters above. With a [voice] it
     * defaults to that source's state.
     */
    state: String? = null,
    inputs: Map<String, Double> = emptyMap(),
    voice: VoiceSource? = null,
    voiceOverrides: VoiceOverrides? = null,
    theme: FxTheme = FxTheme.AUTO,
    paused: Boolean = false,
    reducedMotion: FxReducedMotion = FxReducedMotion.AUTO,
    contentDescription: String? = null,
    maxFps: Double? = null,
    lowPower: FxLowPower = FxLowPower.AUTO,
    onFrame: ((FxFrameStats) -> Unit)? = null,
) {
    val overrides = SinuaCoreProps(pattern = pattern, size = size, highlightFill = highlightFill, highlightLength = highlightLength, hue = hue, ink = ink, saturation = saturation, thickness = thickness, trackOpacity = trackOpacity, period = period, length = length, bounceAmplitude = bounceAmplitude, delay = delay, dotCount = dotCount, dotSize = dotSize, spacing = spacing, glow = glow, noise = noise, pulse = pulse, gradient = gradient, color = color, liquid = liquid, particles = particles, holographic = holographic).toOverrides()
    SinuaView(
        pattern = pattern.id, modifier = modifier, size = size.px, overrides = overrides, speed = speed,
        state = state, inputs = inputs, voice = voice, voiceOverrides = voiceOverrides, theme = theme, paused = paused, reducedMotion = reducedMotion,
        contentDescription = contentDescription, maxFps = maxFps, lowPower = lowPower, onFrame = onFrame,
    )
}

/**
 * Plays an FX Spec (JSON) that must describe a core (`"object": "core"`); any other object
 * draws nothing and calls [onError] (logged when there is none). [state] picks the spec's lifecycle state.
 */
@Composable
fun SinuaCore(
    spec: String,
    modifier: Modifier = Modifier,
    state: String? = null,
    inputs: Map<String, Double> = emptyMap(),
    voiceLevelInput: String? = null,
    voice: VoiceSource? = null,
    voiceOverrides: VoiceOverrides? = null,
    theme: FxTheme = FxTheme.AUTO,
    paused: Boolean = false,
    reducedMotion: FxReducedMotion = FxReducedMotion.AUTO,
    contentDescription: String? = null,
    maxFps: Double? = null,
    lowPower: FxLowPower = FxLowPower.AUTO,
    onError: ((SinuaSpecError) -> Unit)? = null,
    onFrame: ((FxFrameStats) -> Unit)? = null,
) {
    val error = sinuaSpecError(spec, "core")
    if (error != null) {
        LaunchedEffect(error) { onError?.invoke(error) ?: Log.e("SinuaCore", error.toString()) }
        Spacer(modifier)
        return
    }
    SinuaView(
        spec = spec, modifier = modifier, voice = voice, voiceOverrides = voiceOverrides, state = state,
        inputs = inputs, voiceLevelInput = voiceLevelInput, theme = theme, paused = paused, reducedMotion = reducedMotion,
        contentDescription = contentDescription, maxFps = maxFps, lowPower = lowPower, onFrame = onFrame,
    )
}
