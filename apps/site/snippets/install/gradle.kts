// build.gradle.kts -- from the first beta (Maven Central). Until it is published,
// use install/from-source.sh.
dependencies {
    implementation("dev.sinua:sinua-view:0.1.0-beta.4")   // SinuaView, SinuaOrb, ... (Compose)
    implementation("dev.sinua:sinua-core:0.1.0-beta.4")   // the engine + voice sources
}
