# The Web view (React component, mount(), <sinua-view>) and the engine.
npm i @sinua/web @sinua/core
# Voice-reactive? Add the sources. livekit-client only if you use LiveKit.
npm i @sinua/voice
npm i livekit-client
